START

DECLARE variables

CREATE a List of subjects

CREATE a Set of numbers

CREATE a Map of subject scores

IF marks are greater than or equal to 40 THEN
    DISPLAY "Passed"
ELSE
    DISPLAY "Failed"
END IF

FOR each subject in the List
    DISPLAY subject
END FOR

CREATE a function to add two numbers

CALL the function

DISPLAY the result

CHECK for null value

IF value is null THEN
    DISPLAY default value
ELSE
    DISPLAY value
END IF

END