START

count ← 0
todoList ← empty list

WHEN counter button is pressed:
    count ← count + 1
    UPDATE UI

WHEN user enters a todo:
    IF todo is not empty THEN
        ADD todo to todoList
        UPDATE UI
    END IF

GET SharedPreferences

SAVE todoList to local storage

READ saved todoList from local storage

DISPLAY count and todoList

END